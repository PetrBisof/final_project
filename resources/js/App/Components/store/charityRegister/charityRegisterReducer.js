
const registerCharityFalse = {
  type: "CHARITY_FALSE"
}
const registerCharityTRUE = {
  type: "CHARITY_TRUE"
}
  const initialState = {
    showRegisterLink : false,
  }
  export const charityRegisterReducer = (state = initialState, action) => {
    switch(action.type) {
      default:
        return state 
    }
    
  }
  